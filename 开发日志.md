# 开发日志

本文记录「楚歌起 (Chugeqi)」项目的开发情况，面向项目成员与刚接触 C++/JSON/SQL 的新人。内容包括：已实现功能、各文件职能、数据结构、用到的 JSON 与 SQL 知识（新手向）、以及测试方法。

---

## 1. 项目概述

「楚歌起」是一款以楚汉相争、项羽垓下之围至乌江自刎为背景的 **Windows 控制台回合制战斗 RPG 原型**。

技术栈：**C++17 + CMake + 自研 JSON 解析器 + SQLite（官方合并源码）**。

核心设计思想是「数据分层」，把三类数据分开管理，避免混杂：

| 分层 | 内容 | 存储方式 | 说明 |
|------|------|----------|------|
| 静态模板 | 技能池、物品池、敌人模板 | JSON 文件 | 只读定义，程序启动时载入一次，长期持有 |
| 运行时持久状态 | 队伍当前 HP/SP、等级/经验、持有技能/道具 | SQLite（`save.db`） | 需要频繁读写、要存档 |
| 战斗临时数据 | 一场战斗中的战斗者实例 | 内存 | 随战斗结束自动释放 |

---

## 2. 已实现功能详解

### 2.1 战斗系统（`combatSystem`）

- **回合流程**：玩家回合 → 同伴回合 → 敌方回合（敌方暂不会回击），循环至一方全灭或逃跑。
- **普通攻击**：命中率基于双方敏捷，伤害 = `力量 × 威力(10) − 防御`。
- **技能**：消耗 SP，按技能类型分发（伤害 / 治疗），单体或全体。
- **逃跑**：成功率 = `自身敏捷 / (自身敏捷 + 敌方敏捷和)`，受 `disable_run` 开关控制。
- **命中率公式**：`rate = 基础命中率 × 敏捷占比 + 0.05`，夹在 `[0.10, 0.99]`。
- **随机数**：使用 `<random>` 的 `std::mt19937`（`random_device` 播种），统一通过 `roll(upper)` 取 `[0, upper)`。

### 2.2 技能系统（`skill`）

- 基类 `SkillBase`（名称、描述、SP 消耗、作用范围 `AttackScope::Single/All`）。
- 派生三类：
  - `DamageSkill`：威力、附带状态效果、基础命中率。
  - `HealSkill`：治疗量。
  - `ChargingSkill`：属性提升倍率、目标属性、持续回合。
- 技能对象由 `DataLoader` 从 `skill.json` 创建，长期持有；具体执行逻辑统一在 `CombatSystem` 中按 `dynamic_cast` 分派。

### 2.3 角色系统（`character`）

- `Character`：角色基类（只有名字）。
- `Civil`：非战斗 NPC（身份 + 台词，用于对话，暂未接入剧情）。
- `Combatant`：战斗角色，含 id、HP/SP、等级/经验、`baseStats[4]`（力量/魔力/耐力/敏捷）、状态效果、技能列表、背包。

经验与升级：

```
升级所需经验 = 10 × 等级²
升级时：全属性 +1，HP = 100 + 等级×10，SP = 50 + 等级×5（等级上限 99）
战斗胜利经验 = 敌人等级之和 × 10
```

### 2.4 存档系统（`saveManager`）

用 SQLite 持久化队伍状态（详见第 6 节）。提供：

- `hasSave()`：判断是否已有存档。
- `loadParty()`：从存档读回队伍（技能 id 经 `skillPool` 反查为指针）。
- `saveParty()`：把队伍当前状态写入存档。
- `resetSave()`：清空存档。

### 2.5 数据加载（`dataLoader`）

- `loadGameData()`：载入技能池（物品池留空待实现）。
- `loadPartyTemplates()`：首次建档时从 `battle_test.json` 引用主角/同伴模板。
- `loadBattle()`：载入敌方与战斗配置。
- 所有资源用 `std::unique_ptr` 管理，无需手动释放。

### 2.6 战斗日志与界面（`battleLog` / `console`）

- `BattleLog`：用 `std::deque` 保存最近 7 条消息，超出丢弃最旧一条；`render()` 恒定输出 7 行，空时把「(暂无)」居中。
- `console`：平台抽象层，封装清屏 / 按键读取 / UTF-8 初始化，Windows 用 conio/windows，其他平台退化为标准输入。

---

## 3. 各文件职能

| 文件 | 职能 |
|------|------|
| `CMakeLists.txt` | 构建配置：声明 C++17、源文件、SQLite 头文件路径、构建后复制 `data/` |
| `src/combatTestMain.cpp` | 程序入口：加载数据 → 读档 → 战斗 → 存档 → 询问是否重置存档 |
| `src/combatSystem.h/.cpp` | 战斗系统：回合流程、行动执行、命中/伤害计算、界面显示 |
| `src/character.h/.cpp` | 角色：`Character`/`Civil`/`Combatant`，状态效果、背包、升级 |
| `src/skill.h/.cpp` | 技能：`SkillBase` 及伤害/治疗/充能三个派生类，`SkillPool` 类型别名 |
| `src/item.h/.cpp` | 物品：`Item`/`Consumable`/`Equipment`，`ItemPool` 类型别名 |
| `src/dataLoader.h/.cpp` | 静态数据加载：JSON 文件 → 技能池/角色/战斗配置 |
| `src/saveManager.h/.cpp` | 存档：SQLite 读写队伍状态 |
| `src/battleLog.h/.cpp` | 战斗日志：滚动保存与格式化输出 |
| `src/console.h/.cpp` | 平台抽象：清屏、按键、编码初始化 |
| `src/json.h` | 轻量 JSON 解析器（递归下降，只读） |
| `third_party/sqlite/sqlite3.c/.h` | SQLite 官方合并源码，直接编入工程 |
| `data/*.json` | 静态模板：技能池、角色、战斗配置 |

---

## 4. 数据结构解释

### 4.1 类层次（继承）

```
Character            # 只有名字
├── Civil            # 剧情 NPC：身份 + 台词
└── Combatant        # 战斗角色：属性、技能、背包、状态

SkillBase            # 技能基类
├── DamageSkill      # 伤害
├── HealSkill        # 治疗
└── ChargingSkill    # 充能（属性提升）

Item                 # 物品基类
├── Consumable       # 消耗品
└── Equipment        # 装备（四槽位）
```

### 4.2 容器选择

| 容器 | 用途 | 为什么选它 |
|------|------|-----------|
| `std::unordered_map<std::string, ...>` | 技能池/物品池：id → 对象 | 以 id 为键，O(1) 查找 |
| `std::vector` | 队伍、敌人、技能列表 | 顺序访问、索引方便 |
| `std::deque` | 战斗日志 | 双端队列，头部弹出效率高（丢弃最旧消息） |
| `std::unique_ptr` | 拥有资源的对象（技能池、战斗者） | 自动释放，明确所有权，避免内存泄漏 |

### 4.3 状态效果（位标志）

```cpp
enum class StatusEffect : uint16_t {
    None   = 0,
    Burn   = 1 << 0,   // 灼烧
    Slow   = 1 << 1,   // 迟缓
    Stun   = 1 << 2,   // 眩晕
    Charge = 1 << 3,   // 充能
};
```

用「位标志」可以让一个角色同时处于多种状态：`statusFlags` 是多个状态的按位或（如 `Burn | Slow`），判断时用按位与（`statusFlags & Burn`）。`activeStatusEffects` 则保存每个状态的详细数据（持续回合、目标属性、倍率）。

### 4.4 内存所有权

- **拥有者**用 `unique_ptr`（技能池、物品池、队伍、敌方），作用域结束自动释放。
- **借用者**用裸指针（如 `Combatant::skills` 指向技能池、`CombatSystem` 持有的玩家/敌人指针），不负责释放。
- 这样所有权一目了然，也避免了原先手动 `new/delete` 容易漏释放的问题。

---

## 5. JSON 知识（新手向）

### 5.1 什么是 JSON

JSON（JavaScript Object Notation）是一种**文本格式**，用来描述有结构的数据。它只有几种基本类型：

```json
{
  "字符串键": "字符串值",
  "整数": 123,
  "小数": 3.14,
  "布尔": true,
  "空值": null,
  "数组": [1, 2, 3],
  "嵌套对象": { "再嵌套": "也可以" }
}
```

要点：用 `{ }` 表示对象（键值对），`[ ]` 表示数组（有序列表），键必须是带双引号的字符串，值可以是任意类型。

### 5.2 项目中的 JSON 示例

`data/skill.json`（对象，键是技能 id）：

```json
{
  "fireball": {
    "name": "火球",
    "description": "发射火球",
    "type": "damage",
    "cost": 10,
    "power": 8,
    "scope": "single"
  },
  "heal": {
    "name": "治疗",
    "type": "heal",
    "cost": 8,
    "healAmount": 30,
    "scope": "single"
  }
}
```

`data/player.json`（角色模板）：

```json
{
  "id": "player",
  "name": "player",
  "level": 1,
  "hp": 100,
  "sp": 50,
  "exp": 0,
  "baseStats": [10, 5, 8, 12],
  "skills": ["fireball", "heal"]
}
```

`data/battle_test.json`（战斗配置，用引用串起各模板）：

```json
{
  "name": "峡谷遭遇战",
  "first_side": "player",
  "disable_run": false,
  "player_ref": "player.json",
  "companions_ref": ["companion.json"],
  "enemies_ref": ["enemy_test1.json", "enemy_test2.json"]
}
```

### 5.3 手写 JSON 解析器原理（`src/json.h`）

本项目没有引入第三方 JSON 库，而是手写了一个「递归下降」解析器，只有几百行。核心思想：

1. 把整段文本看成一串字符，用一个游标 `pos` 记录当前读到哪里。
2. 跳过空白字符后，看**当前字符**决定怎么解析：
   - `{` → 解析对象（读键、冒号、值，逗号分隔，直到 `}`）
   - `[` → 解析数组（读值，逗号分隔，直到 `]`）
   - `"` → 解析字符串（处理转义 `\"` `\n` `\uXXXX` 等）
   - `t`/`f` → 解析 `true`/`false`
   - `n` → 解析 `null`
   - 数字开头 → 解析数字（整数/小数/科学计数法）
3. 遇到语法错误（如缺引号、缺逗号、多余内容）就抛出 `runtime_error`。

这就是「递归下降」：`parseValue` 会按需递归调用 `parseObject`/`parseArray` 等，天然支持任意嵌套。

解析结果是一个 `json::Value`，提供 `asString()`/`asInt()`/`has()`/`operator[]` 等方法方便取值。注意它是**只读**的，足够本项目加载静态模板使用。

---

## 6. SQL 知识（新手向）

### 6.1 什么是 SQLite 和表

SQLite 是一个**嵌入式数据库**：它不是一个独立的服务器，而是把整个数据库存进一个文件（这里就是 `save.db`），程序通过函数库直接读写。它的核心概念是「表」：

- **表 (table)**：像一张 Excel 表格，有若干列（字段）和若干行（记录）。
- **主键 (PRIMARY KEY)**：唯一标识一行记录的字段，不能重复。
- 关系型数据库用「表 + 外键」表达数据之间的**一对多**关系。

### 6.2 项目的三张表

```
characters          -- 角色基础状态
  id           主键（文本）
  name         名字
  level        等级
  hp / sp      当前生命 / 技力
  exp          经验
  str/mag/endur/agi   四维属性（力量/魔力/耐力/敏捷）

learned_skills       -- 角色已学技能（一个角色可有多条）
  character_id 属于哪个角色
  slot         技能槽位
  skill_id     技能 id
  主键 = (character_id, slot)

inventory            -- 角色背包（一个角色可有多条）
  character_id 属于哪个角色
  item_id      物品 id
  count        数量
  主键 = (character_id, item_id)
```

为什么要拆成三张表，而不是把所有东西塞进一张大表？因为一个角色有**多个**技能、**多个**物品（一对多）。拆表后，每个技能/物品都是单独一行，用 `character_id` 关联回角色，这就是「规范化」——避免在一行里塞下不确定数量的数据。

### 6.3 用到的 SQL 语句

| 语句 | 作用 |
|------|------|
| `CREATE TABLE IF NOT EXISTS ...` | 建表（已存在则跳过） |
| `INSERT INTO ... VALUES (?,?,...)` | 插入一行（`?` 是占位符） |
| `SELECT ... FROM ... ORDER BY ...` | 查询并按某列排序 |
| `DELETE FROM ...` | 删除表中所有行 |
| `BEGIN TRANSACTION` / `COMMIT` | 事务：一批操作要么全部成功、要么全部回滚 |

### 6.4 预编译语句与参数绑定

写存档时不是把值拼进 SQL 字符串（那样既慢又有注入风险），而是：

1. `sqlite3_prepare_v2` 预编译一条带 `?` 占位符的 SQL。
2. `sqlite3_bind_text / sqlite3_bind_int` 把具体值绑定到占位符。
3. `sqlite3_step` 执行。
4. `sqlite3_finalize` 释放语句。

`saveManager.cpp` 里的 `runStatement` 助手把这个流程封装成「绑定参数并执行」的一步。写回技能时，还要通过 `findSkillId` 把「技能指针」反查成「技能 id」再存库（因为数据库只存 id，不存内存指针）。

---

## 7. 测试方法

### 7.1 构建

```powershell
cmake -S . -B build -G "Visual Studio 17 2022" -A x64
cmake --build build --config Release
```

编译无报错即通过基础构建测试。

### 7.2 交互式战斗测试

在 `build/Release/` 下运行 `Chugeqi.exe`，按提示操作：

1. **普通攻击**：选 `[1]攻击` → 选目标，观察命中/伤害与日志滚动。
2. **技能**：选 `[2]技能` → 选技能（火球 / 治疗）→ 选目标，验证 SP 扣除、伤害/治疗数值。
3. **SP 不足**：连续放技能耗尽 SP，验证提示「SP 不足」。
4. **逃跑**：选 `[4]逃跑`，多次验证成功/失败两种结果。
5. **胜利结算**：清光敌人，验证经验发放与升级。

### 7.3 存档测试

1. 首次运行：无 `save.db` 时自动从 JSON 建档。
2. 战斗结束后写回存档；**再次运行**，验证等级/经验被保留（会上升）。
3. 战斗结束选 `Y` 重置存档，**再次运行**验证队伍回到初始模板。

### 7.4 修改 JSON 数据测试

编辑 `data/` 下的 JSON（改数值、加技能），重新运行验证效果，例如：

- 把 `player.json` 的 `baseStats` 调大，观察伤害变化。
- 在 `skill.json` 增加一个 `type: "heal"` 技能并在 `player.json` 的 `skills` 里引用，验证新技能可选。

> 注意：JSON 语法错误（缺逗号、引号）会触发解析器异常，可借此测试错误处理。

### 7.5 已知边界与注意事项

- 敌方 AI 未实现，敌人不会回击，玩家基本不会战败。
- 充能技能、道具、状态效果目前是「未实现」占位。
- 控制台需 UTF-8 编码才能正常显示中文（`console::init()` 已处理）。
- 若换平台（如 Linux），`console` 会退化为标准输入，但 `conio` 交互体验不同，需注意。

---

## 8. 已知问题与后续计划

- [ ] 实现敌人 AI（敌人回击）
- [ ] 实现状态效果结算（灼烧/迟缓/眩晕/充能）
- [ ] 实现道具使用与装备加成
- [ ] 接入剧情/任务系统（利用 `Civil` 与幕次结构）
- [ ] 补充自动化单元测试（目前为手工交互测试）
- [ ] 用 `type.h` 的历史版本整理属性命名（现 `type.h` 已删除）

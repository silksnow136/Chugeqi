# 开发日志

本文记录「楚歌起 (Chugeqi)」的开发进度与现状，面向项目成员。

> 历史版本已归档至 `archive/`（`开发日志_旧版.md`、`开发日志_2026-09-13.md`、`开发日志_2026-09-14.md`）。

---

## 1. 项目概述

「楚歌起」是以楚汉相争、项羽垓下之围至乌江自刎为背景的控制台回合制 RPG，由两个项目合并而成：

- **Chugeqi**：战斗内核（回合制战斗、角色/技能/物品、JSON 存档、JSON 数据加载）。
- **TheFalloftheOverlord**：剧情外壳（四幕剧情、场景、对话、地图、逐字打印）。

技术栈：C++17 + CMake + 自研 JSON 解析器。存档为 JSON 文件。

核心设计思想是「数据分层」：

| 分层 | 内容 | 存储方式 |
|------|------|----------|
| 静态模板 | 技能池、物品池、装备池、剧情文本、对话文本、敌人模板、战斗配置 | JSON 文件，启动时载入一次 |
| 运行时持久状态 | 队伍 HP/SP、等级/经验、技能/道具、装备、剧情进度（scene/branch/gold） | JSON（`saves/save_<slot>.json`） |
| 临时数据 | 一场战斗的战斗者、场景状态、支线进度 | 内存，随流程释放 |

---

## 2. 开发进度

### 2.1 战斗原型（Chugeqi 早期）

- 回合制战斗：玩家 + 同伴手动操作，普攻 / 伤害技能 / 治疗技能 / 逃跑。
- 命中与伤害计算：基于敏捷的命中率、`力量 × 威力 − 防御` 的伤害公式。
- 角色成长、技能/物品类层次、自研 JSON 解析器、SQLite 三表存档。
- 平台抽象层 `console`（清屏 / 按键 / 编码）。

### 2.2 状态效果大修

- 实现灼烧 / 迟缓 / 眩晕 / 充能四种状态效果。
- 结算统一到战斗主循环每轮开始（灼烧扣血、迟缓提示）与轮末（持续回合递减）。
- 属性修正（迟缓降敏捷、充能提属性）集中在 `Combatant::getEffectiveStat`。
- 保留并完善敌人 AI（回击、技能选择、集火残血）与我方 AI 托管。

### 2.3 TheFalloftheOverlord 移植

- 全源码 GBK → UTF-8，移除 `windows.h` / `conio.h` 依赖，接入 `console` 平台抽象。
- 构建由 `.sln` / `.vcxproj` 改为 CMake。
- 剧情文本抽离为 `data/story.json`，由 `loadStory` 解析（旁白/对话/分支结构）。

### 2.4 合并两个仓库

- 用 `git merge --allow-unrelated-histories` 把 TheFalloftheOverlord 合并进 Chugeqi，保留两个仓库的完整提交历史。
- 统一 console 层、去重 `json.h`、合并 CMakeLists、以 `main.cpp`（剧情）为主入口。

### 2.5 目录重构 + 统一数据加载

- 源码按职责拆为 `src/core/`（平台抽象 + JSON 解析）、`src/combat/`（战斗）、`src/story/`（剧情）。
- 剧情数据结构（`StoryLine`/`Scene`/`StoryData`）与 `loadStory` 并入 `DataLoader`。
- CMake 拆出多个目标。

### 2.6 装备系统

- `item.h` 新增 `Equipment`（`EquipmentSlot`：Armor/Weapon/Shoes/Accessory）与 `bonus[4]` 属性加成。
- `Combatant` 新增四槽位管理：`equipItem` / `unequipItem` / `getEquippedItemId`，属性加成叠加进 `getEffectiveStat`。
- `DataLoader::loadItems` 支持解析装备（`type=equipment` + `slot` + `bonus`）、消耗品、材料，并按 `category` 分类。

### 2.7 药店系统

- `PharManager` 实现：`showShop`、`buyItem`（金币购买）、`useItem`（回 HP/SP）、`showMedicineBag`。
- `Game` 持有 `gold` 与 `ItemPool`，`SceneManager` 在药店交互时按需创建 `PharManager`。

### 2.8 对话系统 JSON 化

- `TalkManager` 从 `data/talk.json` 读取并逐句播放，按 `scene_id`/`branch_id`/`character_id` 组织。

### 2.9 网格地图系统（MapGrid）

- `MapGrid`：二维网格，每格固定 4 列宽（含中文宽度计算与补齐）；WASD 移动、边界/碰撞检测。
- `TileType`：EMPTY/WALL/PLAYER/FRIEND/ENEMY/PHARMACY/DOOR/ITEM/ADVANCE。
- 交互：友方→对话、敌方→战斗、药店→购买、物品→拾取（消失）、门→通行、ADVANCE→幕次跳转。
- 建造方法：`buildRoom`（房间+门）/`buildFence`（栅栏）/`buildCheval`（拒马）/`buildWater`（水域）。
- 渲染防闪烁：首屏 `clearScreen`，后续 `moveCursor` 覆盖 + `clearToEnd` 清残留，`setCursorVisible` 隐藏光标。

### 2.10 平台层扩展 + Linux 兼容

- `console` 扩展到 15 函数：新增 `moveCursor` / `setCursorVisible` / `clearToEnd` / `enterRaw` / `restoreCanonical` / `drainInput` / `readLine`。
- Linux 分支：ANSI 清屏、方向键转义序列解析（`KEY_UP/DOWN/LEFT/RIGHT`）。
- 添加 `.gitattributes` 统一 LF 换行符。

### 2.11 目录重构：数据层独立

- `dataLoader` 与 `saveManager` 统一迁移到 `src/data/`。
- `DataLoader::loadGameData` 统一加载技能池、物品池；`loadBattle` 支持 `first_side`/`disable_run`/`disable_items` 配置。

### 2.12 存档 JSON 化 + 存读档界面

- 放弃 SQLite，`SaveManager` 改为 JSON 双槽位（`saves/save_1.json`、`save_2.json`）。
- 存档内容：`scene`/`branch`/`gold` 元信息 + `party`（每名角色 `id/name/level/hp/sp/maxHp/maxSp/exp/str/mag/end/agi/skills/items/equip`）。
- `Game` 提供 `doSave`/`doLoad`/`saveMenu`/`slotMenu`，主菜单「继续征途」可打开存读档界面（覆盖需确认）。
- `SaveManager::getSlotInfo` 仅读摘要供界面展示，无需加载完整队伍。

### 2.13 剧情 ↔ 战斗衔接

- `StoryLine` 新增 `battle`/`battleId` 字段，`story.json` 三处「打斗」标记战斗触发。
- `SceneManager::startStoryBattle` 根据 `battleId` 加载 `battle_<id>.json`，构造 `CombatSystem` 并运行。
- `playLines`/`playChoice` 处理战斗结果：失败则中断后续剧情。
- 三场战斗：第二幕 `wangjian`（王翦）、第三幕 `qinshiyue`（秦时月）、第四幕 `final`（王翦/杨喜/吕胜/杨武）。

### 2.14 网格地图接入主游戏（SceneMap）

- 新增 `SceneMap.h/.cpp`：按场景 id/分支构建四幕地图布局，`runSceneMap` 运行 WASD 交互循环。
- 交互回调接入游戏系统：`onTalk`（接 `TalkManager`）、`onPharmacy`（`PharManager`）、`onBattle`（`CombatSystem`）、`onItem`（拾取）、`onAdvance`（幕次跳转）。

### 2.15 删除锻造 + 清理旧场景表现系统

- 完全删掉锻造：`ForgeManager` 及其 `FORGE` 状态、`FORGE` tile 一并移除。
- 删除旧版静态 ASCII 地图 `map.h/.cpp`、早期文字标题 `BackGround.h/.cpp`、旧对话系统死代码。
- 删除遗留的命令行接口（`map`/`south`/`north` 等），主菜单收敛为「开始/继续/退出/帮助」。

### 2.16 CMake 收敛为 3 目标

- 移除重复的地图 demo 与测试入口（`mapTest`、`gaiXiaMap`、`huaiRiverMap`、`dongchengMap`、`wujiangMap`）。
- 现保留：`Chugeqi`（主游戏）、`combatTest`（战斗测试）、`portalTest`（传送门地图链测试）。

### 2.17 角色 maxHP / maxSP

- `Combatant` 新增 `maxHp`/`maxSp` 属性（构造默认取初始 HP/SP），升级时全属性 +1 并恢复满 HP/SP。
- `saveManager` 读写 `maxHp`/`maxSp`（旧存档无此字段时回退 -1 兼容）。

### 2.18 战斗系统拆分多文件

- `combatSystem.cpp` 拆为四份：核心（状态/回合流程）、`combatSystemUI.cpp`（界面）、`combatSystemTurns.cpp`（回合）、`combatSystemActions.cpp`（行动）。
- 菜单/目标选择/状态显示辅助改为私有静态成员，便于按职责分文件实现。

### 2.19 战斗内道具使用

- 实现 `performItem`（按物品 ID 解析消耗品效果，扣 HP/SP 并消耗数量）与 `useItemInBattle`（战斗菜单列出可用药品，`use+编号` 使用）。
- 接入回合循环：使用道具算一次行动；`disable_items` 配置可禁止。

### 2.20 模块化拆分（MapLayouts / backpack / SideQuest）

- **MapLayouts**：四幕主线地图 + 支线子地图的静态布局独立（只摆格子，不含交互）。
- **backpack**：背包/物品工具独立（装配/卸下/使用/属性查看），地图内按 `B` 调用。
- **SideQuest + QuestState**：支线任务逻辑与主线场景推进解耦，只依赖 `QuestState` + `Game`/`Combatant`。

### 2.21 对话 JSON 化：按名查找 + simple 表

- `talk.json` 增加 `simple` 表（NPC 名 → 单句文本），供未接入主对话的 NPC 快速复用。
- `TalkManager::talkCharacterExternal` 按 `scene_id`/`branch_id`/`name` 查找人物对话，未命中则回落到 `playSimpleTalk`。
- `TalkManager` 缓存 `talk.json` 解析结果，避免重复解析。

### 2.22 传送门地图链（PORTAL）

- `MapGrid` 新增 `PORTAL` tile 与 `buildPortal`（`PortalDir`：上下左右），支持横纵向链式传送。
- `SceneMap` 处理传送：`onEnterMap` 重置子地图状态、按目标名 `buildNamedMap` 重建网格并重新绑定回调。
- 衔接子地图：垓下营地 ↔ 营外荒郊、淮河 ↔ 阴陵一/二/三。

### 2.23 支线任务系统

- **支线一「收拢军心」**（第一幕）：老兵寻回逃兵（营外荒郊 3 名）、粮官巡南门（击退汉军哨骑）、粮仓分配粮草（士兵/战马/留存突围三选一），共同影响军心值与突围旁白。
- **支线二「阴陵迷境」**（第二幕）：田夫指路解锁入口、渡河图显现浅滩、蓑衣避寒水（无蓑衣下三层扣血）、击败灌婴 BOSS 开启归路。
- `SideQuest::tryHandleTalk` / `onBattleWon` / `canEnterPortal` / `advanceNarration` 窄接口接入 `SceneMap`。

### 2.24 幕次旁白 + 拾取物品 ID

- `story.json` 每幕新增 `advance` 字段，`ADVANCE` 格触发时播放幕次跳转旁白（`SceneManager::getAdvancePrompt`）。
- 地图拾取改为直接存物品 ID（`Tile.name` 即物品 ID），`SceneMap` 按 ID 从 `ItemPool` 取名称展示。

### 2.25 并行自动化测试 + 缺陷修复

- 用多个并行 subagent 对主游戏 / 战斗 / 传送门 / 数据完整性 / 健壮性做黑盒测试，修复了一批崩溃与死循环：
  - 传送门 `portalTest` 回调内重建网格导致的 use-after-free（改为延迟重建）。
  - 损坏 / 缺字段存档在存读档界面崩溃（`getSlotInfo` 加异常保护，按空档处理）。
  - stdin EOF 导致主菜单 / 战斗菜单死循环（改为优雅退出）。
  - 战斗道具指令 `u`/`u1` 触发 `substr` 越界；`combatTest` 未传 `itemPool` 导致道具失效。
  - 眩晕对友方无效（后手眩晕在轮末被清掉，改为在目标回合被跳过时消费）。
  - 禁用逃跑的战斗仍显示 `[4]逃跑`；向上/向左传送门文字被横条覆盖。

### 2.26 数值平衡调整

- 命中率：由「基础命中 × 敏捷占比」改为「基础命中(0.85) + 敏捷差 × 2%」，上限 92%、下限 10%。
- 灼烧：由「目标魔力 × 10」改为「目标最大生命的 10%」，与魔力无关、量级更合理。

### 2.27 击败敌人后 tile 消失

- 交互回调 `onInteract` 增加目标所在行/列参数，新增 `MapGrid::clearTile`。
- `SceneMap` 遇敌胜利后清除该敌人 tile，使其从地图上消失。

### 2.28 精简过度设计

- `json::Value` 增加 `set`/`push`/`object`/`array`/`stringify`，存档序列化改为构建 Value 树，读写统一为同一套 JSON；删除手拼 JSON 字符串。
- 删除 `performAttack` 恒为 true 的 `isNormalAttack` 死参数、战斗界面 `[ASCII Art 预留区域]` 占位框。
- `displayBattle` 统一为每次 `clearScreen`，删除 `firstRender` 与 `moveCursor` 覆盖重绘。
- `console` 移除 `moveCursor`/`pauseEsc`，`restoreCanonical` 转为内部实现（对外 15 → 12 函数）。
- 删除 `utf8Chars` 辅助函数，切分逻辑并入 `splitByWidth`。

### 2.29 地图持久状态 + 存读档改进

- 新增 `WorldState`（当前地图名 / 玩家坐标 / 每张地图已清除格子），由 `Game` 持有，跨传送门 / 读档保留。
- 击败敌人、拾取道具标记为已清除，重建地图时 `applyCleared` 恢复；往返传送不刷新敌人与道具。
- 存档新增 `world` 字段；读档时若位于地图内，`resumeScene` 直接进地图并恢复坐标，跳过幕次剧情。
- 地图内新增 `E` 键存读档，补全按键提示（`B=背包 E=存读档`）。

### 2.30 去魔法化：砍属性与状态、精简装备与技能

- 属性由四项（力量/魔力/耐力/敏捷）精简为三项（力量/耐力/敏捷），删除「魔力」，伤害始终以力量结算。
- 状态效果精简为「灼烧 / 眩晕」两种；删除「迟缓」与「充能」，连带删除 `ChargingSkill` 类与充能相关结算分支。
- 装备槽位由四槽（防具/武器/鞋子/配饰）精简为两槽（防具/武器），`bonus` 与 `baseStats` 均由 4 项改 3 项。
- 技能去魔法化：删除「聚力」「寒冰」，`火球`→`火箭`（灼烧）、`奔雷`→`重击`（眩晕），并让灼烧/眩晕在更多敌人（赤侯/斥候/王翳/吕马童等）与玩家「重击」中出场。
- 存档字段由 `str/mag/end/agi` 改为 `str/end/agi`，装备槽位与技能池同步收敛。

---

## 3. 当前功能现状

### 3.1 剧情系统（`src/story`）

- 四幕剧情（垓下/淮河/东城/乌江），从 `data/story/story.json` 加载。
- `SceneManager` 播放叙事：`playLines` / `playChoice`（第二幕分支）、`startStoryBattle`（战斗接入）。
- `printWords` 逐字打印（含 UTF-8 中文按 3 字节切分）、`nextLine` 自动/手动切换（ESC）、按 q 加速。
- 幕次跳转旁白（`advance`）由 `getAdvancePrompt` 提供。

### 3.2 战斗系统（`src/combat`）

- 回合流程：玩家 → 同伴 → 敌方，循环至一方全灭或逃跑。
- 普通攻击、伤害/治疗技能、逃跑（可被战斗配置禁用）、战斗内使用道具。
- 敌人 AI：回击，约 60% 概率施放技能、集火残血；我方支持 AI 托管（ESC 退出）。
- 命中率：基础命中 0.85 + 敏捷差 ×2%（更快者命中更高，上限 92%、下限 10%）。
- 状态效果：灼烧（回合开始扣最大生命 10%）、眩晕（跳过行动）。
- 装备：两槽位（防具/武器）属性加成；经验与升级、`maxHP`/`maxSP`、JSON 存档。

### 3.3 物品 / 装备（`src/combat` + `src/story/backpack`）

- `Item` 基类 → `Consumable`（回 HP/SP）、`Equipment`（两槽位属性加成）。
- `ItemPool` 由 `DataLoader` 持有，按 `id` 索引。
- 背包界面（`backpack.cpp`）支持装配/卸下/使用药水/查看属性，按 装备→药水→材料 分组显示。

### 3.4 地图 / 支线（`src/story`）

- `MapGrid`：网格地图 + 建造 + `PORTAL` 传送门 + 防闪烁渲染。
- `MapLayouts`：四幕主线地图（垓下营地/淮河/东城/乌江）+ 子地图（营外荒郊/阴陵一/二/三）布局。
- `SceneMap`：统一 `onInteract` 回调接入对话/药店/战斗/拾取/跳幕/传送；`B` 背包、`E` 存读档、`ESC` 退出。
- `WorldState`：地图持久状态（地图名/玩家坐标/已清除格子），传送与读档间保留敌人、道具。
- `SideQuest` + `QuestState`：两套支线任务。

### 3.5 平台抽象与数据层（`src/core`、`src/data`）

- `console`：`init` / `clearScreen` / `readKey` / `pause` / `setColor` / `sleep` / `kbhit` / `setCursorVisible` / `clearToEnd` / `enterRaw` / `drainInput` / `readLine`。
- `json.h`：轻量递归下降 JSON 解析器（支持解析与序列化）。
- `dataLoader`：统一加载技能、物品/装备、角色、战斗配置、剧情。
- `saveManager`：JSON 双槽位存档/读档/删档，含槽位摘要读取；存队伍 + 剧情进度 + 地图状态（`world`）。

---

## 4. 目录结构与文件职能

| 文件 | 职能 |
|------|------|
| `src/main.cpp` | 主入口：创建 `Game` 并运行剧情 |
| `src/core/console.h/.cpp` | 平台抽象：清屏、按键、颜色、延时、编码、raw 模式、整行输入 |
| `src/core/json.h` | 轻量 JSON 解析器 + 序列化 |
| `src/data/dataLoader.h/.cpp` | 统一数据加载：技能/物品(装备)/角色/战斗配置/剧情 |
| `src/data/saveManager.h/.cpp` | JSON 存档（2 槽位，队伍 + 剧情进度 + 地图状态） |
| `src/combat/combatSystem.h/.cpp` | 战斗系统：回合流程、AI、状态效果结算（核心） |
| `src/combat/combatSystemUI.cpp` | 战斗界面：渲染、菜单、目标选择、状态显示 |
| `src/combat/combatSystemTurns.cpp` | 回合处理：玩家/同伴/敌方/AI 托管 |
| `src/combat/combatSystemActions.cpp` | 行动执行：攻击/技能/道具/逃跑 |
| `src/combat/character.h/.cpp` | 角色：`Character`/`Combatant`，属性/状态/背包/装备槽位/maxHP/maxSP |
| `src/combat/skill.h/.cpp` | 技能：伤害/治疗 |
| `src/combat/item.h/.cpp` | 物品：消耗品/装备 |
| `src/combat/battleLog.h/.cpp` | 战斗日志 |
| `src/combat/combatTestMain.cpp` | 战斗测试入口 |
| `src/story/Game.h/.cpp` | 游戏外壳：主循环、指令处理、玩家/金币/物品池、地图状态、存读档 |
| `src/story/SceneManager.h/.cpp` | 剧情播放、幕次跳转旁白、战斗接入 |
| `src/story/SceneMap.h/.cpp` | 场景地图交互循环（WASD），回调接入各模块 |
| `src/story/MapGrid.h/.cpp` | 网格地图系统：移动/碰撞/交互/建造/传送门 |
| `src/story/MapLayouts.h/.cpp` | 四幕主线地图 + 支线子地图布局 |
| `src/story/TalkManager.h/.cpp` | 对话系统（从 talk.json 加载，simple + scenes） |
| `src/story/backpack.h/.cpp` | 背包/物品工具：装配/卸下/使用/属性查看 |
| `src/story/SideQuest.h/.cpp` | 支线任务逻辑：收拢军心 / 阴陵迷境 |
| `src/story/QuestState.h` | 支线任务进度状态 |
| `src/story/WorldState.h` | 地图持久状态：地图名/玩家坐标/已清除格子 |
| `src/story/PharManager.h/.cpp` | 药店：购买/使用药品/查看背包 |
| `src/story/portalTestMain.cpp` | 传送门地图链测试入口 |
| `data/core/skill.json` / `item.json` | 静态模板：技能池、物品池（消耗品 + 装备） |
| `data/story/story.json` / `talk.json` | 静态模板：四幕剧情、场景对话 |
| `data/characters/*.json` | 静态模板：主角/同伴/敌人角色模板 |
| `data/battles/battle_*.json` | 静态模板：战斗配置（引用 characters/ 下的角色） |

---

## 5. 待办事项

- [ ] 任务系统（当前仅两套硬编码支线：收拢军心 / 阴陵迷境）。
- [ ] 补充自动化测试（目前为手工交互测试）。
- [ ] 同伴阵亡后的战斗外复活（药店/背包治疗仅作用于主角）。

---

## 6. 测试方法

### 6.1 构建

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

编译无报错即通过基础构建测试。共 3 个目标：`Chugeqi`、`combatTest`、`portalTest`。

### 6.2 剧情测试（`Chugeqi`）

1. 启动后输入 `1` 开始游戏，观察四幕剧情逐字播放。
2. 播放中按 ESC 切换自动/手动；按 q 加速当前对话。
3. 第二幕田父指路处，输入 `A`/`B` 验证两条分支（王翦/沼泽）剧情不同。
4. 剧情行进到「打斗」处触发真实战斗（王翦/秦时月/最终决战），失败则剧情中断。
5. 进入场景后为网格地图：WASD 移动，走向 NPC 对话、药店购买、遇敌战斗、拾取、传送门（白色箭头）、幕次跳转点（帅帐/北渡/突围/渡船）。
6. 地图内按 `B` 打开背包、`E` 存读档，`ESC` 退出场景；击败敌人后该敌人消失，往返传送不刷新敌人/道具。

### 6.3 支线任务测试（`Chugeqi`）

1. 第一幕找「老兵」接委托 → 出北门（传送门）进「营外荒郊」劝回 3 名逃兵 → 回老兵处交付。
2. 第一幕找「粮官」接委托 → 出南门击败「汉军哨骑」→ 回粮官处交付。
3. 第一幕找「粮仓」选择粮草分配方案（1/2/3），观察军心变化与突围旁白插话。
4. 第二幕找「田夫」解锁阴陵入口 → 进「阴陵一/二/三」，持「渡河图」辨认浅滩、未穿「蓑衣」下三层扣血、击败「灌婴」开启归路。

### 6.4 战斗测试（`combatTest`）

1. 普通攻击、技能（霸王斩/横扫千军/重击，同伴为治疗/群疗）、逃跑。
2. 状态效果：敌人火箭附加灼烧（回合开始扣血）、重击附加眩晕，观察状态栏显示。
3. 敌人 AI：敌方会回击；按菜单开启我方 AI 托管，ESC 退出。
4. 道具使用：战斗菜单选道具，`use+编号` 使用药品（恢复 HP/SP）。
5. 存档：战斗结束后写回存档文件，再次运行验证等级保留。

### 6.5 存读档测试（`Chugeqi`）

1. 主菜单输入 `2`（继续征途）打开存读档界面；地图内按 `E` 亦可打开。
2. 选择存档位 → 存档（覆盖需 y/n 确认）/ 读档 / 返回。
3. 读档后验证队伍等级、金币、场景进度（scene/branch）恢复；若存档时位于地图内，读档直接进入该地图并恢复坐标，不再重播幕次剧情。
4. 存档文件生成于 `saves/save_<slot>.json`，可直接查看 JSON 内容（含 `maxHp`/`maxSp`/`equip`/`world`）。

### 6.6 传送门地图链测试（`portalTest`）

1. WASD 控制项羽移动；走向四个方向传送门（白色箭头），传送到相邻地图。
2. 6 张地图（3 列 × 2 行）上下左右互通，验证链式传送与重建后回调绑定。
3. ESC 退出。

### 6.7 修改 JSON 数据测试

- 改 `data/story/story.json` 的文本/颜色/分支/`battleId`/`advance`，重跑 `Chugeqi` 验证剧情与战斗变化。
- 改 `data/core/skill.json` 或 `data/characters/player.json` 的数值，重跑 `combatTest` 验证战斗数值变化。
- 改 `data/core/item.json` 装备 `bonus` 或药水 `healHP`，重跑 `Chugeqi` 开背包验证。
- 改 `data/battles/battle_*.json` 的 `enemies_ref`/`disable_run`，验证剧情战斗配置生效。

> JSON 语法错误会触发解析器异常，可借此验证错误处理。

---

## 7. 已知遗留 / 技术债

### 7.1 地图遭遇战为单敌人

- 每场地图遇敌只配置单个敌人（玩家 + 同伴 2v1），暂无多敌人编队的地图遭遇配置。

### 7.2 同伴死亡后无法战斗外复活

- 药店与背包的治疗仅作用于主角，同伴阵亡后需读档或新开游戏恢复。

### 7.3 无自动化测试

- 目前所有验证依赖手工交互，缺少单元测试 / 集成测试。

# 开发日志

本文记录「楚歌起 (Chugeqi)」的开发进度与现状，面向项目成员。

> 历史版本已归档至 `archive/`（`开发日志_旧版.md`、`开发日志_2026-09-13.md`）。

---

## 1. 项目概述

「楚歌起」是以楚汉相争、项羽垓下之围至乌江自刎为背景的控制台回合制 RPG，由两个项目合并而成：

- **Chugeqi**：战斗内核（回合制战斗、角色/技能/物品、JSON 存档、JSON 数据加载）。
- **TheFalloftheOverlord**：剧情外壳（四幕剧情、场景、对话、地图、逐字打印）。

技术栈：C++17 + CMake + 自研 JSON 解析器。存档由早期 SQLite 改为 JSON 文件。

核心设计思想是「数据分层」：

| 分层 | 内容 | 存储方式 |
|------|------|----------|
| 静态模板 | 技能池、物品池、装备池、剧情文本、对话文本、敌人模板、战斗配置 | JSON 文件，启动时载入一次 |
| 运行时持久状态 | 队伍 HP/SP、等级/经验、技能/道具、装备、剧情进度（scene/branch/gold） | JSON（`saves/save_<slot>.json`） |
| 临时数据 | 一场战斗的战斗者、场景状态 | 内存，随流程释放 |

---

## 2. 开发进度

### 2.1 战斗原型（Chugeqi 早期）

- 回合制战斗：玩家 + 同伴手动操作，普攻 / 伤害技能 / 治疗技能 / 逃跑。
- 命中与伤害计算：基于敏捷的命中率、`力量 × 威力 − 防御` 的伤害公式。
- 角色成长、技能/物品类层次、自研 JSON 解析器、SQLite 三表存档。
- 平台抽象层 `console`（清屏 / 按键 / 编码）。

### 2.2 状态效果大修

- 实现灼烧 / 迟缓 / 眩晕 / 充能四种状态效果。
- 结算统一到战斗主循环每轮开始（灼烧扣血、迟缓提示）与轮末（持续回合递减）。
- 属性修正（迟缓降敏捷、充能提属性）集中在 `Combatant::getEffectiveStat`。
- 保留并完善敌人 AI（回击、技能选择、集火残血）与我方 AI 托管。

### 2.3 TheFalloftheOverlord 移植

- 全源码 GBK → UTF-8，移除 `windows.h` / `conio.h` 依赖，接入 `console` 平台抽象。
- 构建由 `.sln` / `.vcxproj` 改为 CMake。
- 剧情文本抽离为 `data/story.json`，由 `loadStory` 解析（旁白/对话/分支结构）。

### 2.4 合并两个仓库

- 用 `git merge --allow-unrelated-histories` 把 TheFalloftheOverlord 合并进 Chugeqi，保留两个仓库的完整提交历史。
- 统一 console 层、去重 `json.h`、合并 CMakeLists、以 `main.cpp`（剧情）为主入口。

### 2.5 目录重构 + 统一数据加载

- 源码按职责拆为 `src/core/`（平台抽象 + JSON 解析）、`src/combat/`（战斗）、`src/story/`（剧情）。
- 剧情数据结构（`StoryLine`/`Scene`/`StoryData`）与 `loadStory` 并入 `DataLoader`。
- CMake 拆出多个目标：`Chugeqi`（主游戏）与 `combatTest`（战斗测试）等。

### 2.6 装备系统

- `item.h` 新增 `Equipment`（`EquipmentSlot`：Armor/Weapon/Shoes/Accessory）与 `bonus[4]` 属性加成。
- `Combatant` 新增四槽位管理：`equipItem` / `unequipItem` / `getEquippedItemId`，属性加成叠加进 `getEffectiveStat`。
- `DataLoader::loadItems` 支持解析装备（`type=equipment` + `slot` + `bonus`）、消耗品、材料，并按 `category` 分类。

### 2.7 药店系统

- `PharManager` 实现：`showShop`、`buyItem`（金币购买）、`useItem`（回 HP/SP）、`showMedicineBag`。
- `Game` 持有 `gold` 与 `ItemPool`，`SceneManager` 在 PHARMACY 状态下按需创建 `PharManager`。

### 2.8 对话系统 JSON 化

- `TalkManager::loadDialogue` 按 `scene_id`/`branch_id`/`character_id` 从 `data/talk.json` 读取并逐句播放。
- 第二幕对话按 `branches` 区分（分支 A 王翦/汉军、分支 B 副将）。

### 2.9 网格地图系统（MapGrid）

- `MapGrid`：二维网格，每格固定 4 列宽（含中文宽度计算与补齐）；WASD 移动、边界/碰撞检测。
- `TileType`：EMPTY/WALL/PLAYER/FRIEND/ENEMY/PHARMACY/FORGE/DOOR/ITEM/ADVANCE。
- 交互：友方→对话、敌方→战斗、药店→购买、铁匠铺→锻造、物品→拾取（消失）、门→通行、ADVANCE→幕次跳转。
- 建造方法：`buildRoom`（房间+门）/`buildFence`（栅栏）/`buildCheval`（拒马）/`buildWater`（水域）。
- 渲染防闪烁：首屏 `clearScreen`，后续 `moveCursor` 覆盖 + `clearToEnd` 清残留，`setCursorVisible` 隐藏光标。

### 2.10 平台层扩展 + Linux 兼容

- `console` 扩展到 11 函数：新增 `moveCursor` / `setCursorVisible` / `clearToEnd`；后补充 `enterRaw` / `drainInput` 管理 raw 模式与输入残留。
- Linux 分支：ANSI 清屏、方向键转义序列解析（`KEY_UP/DOWN/LEFT/RIGHT`）。
- 添加 `.gitattributes` 统一 LF 换行符。

### 2.11 目录重构：数据层独立

- `dataLoader` 与 `saveManager` 从 `src/core/`、`src/combat/` 统一迁移到 `src/data/`。
- `DataLoader::loadGameData` 统一加载技能池、物品池；`loadBattle` 支持 `first_side`/`disable_run`/`disable_items` 配置。

### 2.12 存档 JSON 化 + 存读档界面

- 放弃 SQLite，`SaveManager` 改为 JSON 双槽位（`saves/save_1.json`、`save_2.json`）。
- 存档内容：`scene`/`branch`/`gold` 元信息 + `party`（每名角色 `id/name/level/hp/sp/exp/str/mag/end/agi/skills/items/equip`）。
- `Game` 提供 `doSave`/`doLoad`/`saveMenu`/`slotMenu`，主菜单「继续征途」与地图内 `F` 键均可打开存读档界面（覆盖需确认）。
- `SaveManager::getSlotInfo` 仅读摘要供界面展示，无需加载完整队伍。

### 2.13 剧情 ↔ 战斗衔接

- `StoryLine` 新增 `battle`/`battleId` 字段，`story.json` 三处「打斗」标记战斗触发。
- `SceneManager::startStoryBattle` 根据 `battleId` 加载 `battle_<id>.json`，构造 `CombatSystem` 并运行。
- `playLines`/`playChoice` 处理战斗结果：失败则中断后续剧情（修复「战斗失败程序无法继续」）。
- 三场战斗：第二幕 `wangjian`（王翦）、第三幕 `qinshiyue`（秦时月）、第四幕 `final`（王翦/杨喜/吕胜/杨武）。

### 2.14 网格地图接入主游戏（SceneMap）

- 新增 `SceneMap.h/.cpp`：`buildSceneMap` 按场景 id/分支构建四幕地图布局，`runSceneMap` 运行 WASD 交互循环。
- 交互回调接入游戏系统：`onTalk`（接 `TalkManager`/简易文本）、`onPharmacy`（`PharManager`）、`onForge`（`ForgeManager`）、`onBattle`（`CombatSystem`）、`onItem`（拾取）、`onAdvance`（幕次跳转 + 过渡旁白）。
- `SceneManager::showSceneManager` 改为调用 `SceneMap::runSceneMap`；地图内 `B` 背包、`F` 存读档、`ESC` 退出。

---

## 3. 当前功能现状

### 3.1 剧情系统（`src/story`）

- 四幕剧情（垓下/淮河/东城/乌江），从 `data/story.json` 加载。
- `SceneManager` 场景状态机：`ORIGIN_SCENE` / `TALK` / `PHARMACY` / `FORGE`。
- `printWords` 逐字打印（含 UTF-8 中文按 3 字节切分）、`nextLine` 自动/手动切换（ESC）、按 q 加速。
- 第二幕分支（田父指路 A/B）由 `StoryChoice`/`StoryOption` 表达，`playChoice` 处理选择。
- 剧情行可触发战斗（`battle`/`battleId`），由 `startStoryBattle` 接入 `CombatSystem`。

### 3.2 战斗系统（`src/combat`）

- 回合流程：玩家 → 同伴 → 敌方，循环至一方全灭或逃跑。
- 普通攻击、伤害/治疗/充能技能、逃跑（可被战斗配置禁用）。
- 敌人 AI：回击，约 60% 概率施放技能、集火残血；我方支持 AI 托管（ESC 退出）。
- 状态效果：灼烧（回合开始扣血）、迟缓（敏捷 ×3/5）、眩晕（跳过行动）、充能（属性 ×倍率）。
- 装备：四槽位属性加成；经验与升级、JSON 存档。

### 3.3 物品 / 装备（`src/combat`）

- `Item` 基类 → `Consumable`（回 HP/SP、临时属性加成）、`Equipment`（四槽位属性加成）。
- `ItemPool` 由 `DataLoader` 持有，按 `id` 索引。
- 背包：`showBackpack`（`MapGrid.cpp` 内）支持装配/卸下/使用。

### 3.4 平台抽象与数据层（`src/core`、`src/data`）

- `console`：`init` / `clearScreen` / `readKey` / `pause` / `pauseEsc` / `setColor` / `sleep` / `kbhit` / `moveCursor` / `setCursorVisible` / `clearToEnd` / `enterRaw` / `drainInput`。
- `json.h`：轻量递归下降 JSON 解析器（只读）。
- `dataLoader`：统一加载技能、物品/装备、角色、战斗配置、剧情。
- `saveManager`：JSON 双槽位存档/读档/删档，含槽位摘要读取。

---

## 4. 目录结构与文件职能

| 文件 | 职能 |
|------|------|
| `src/main.cpp` | 主入口：创建 `Game` 并运行剧情 |
| `src/core/console.h/.cpp` | 平台抽象：清屏、按键、颜色、延时、编码、光标控制、raw 模式 |
| `src/core/json.h` | 轻量 JSON 解析器 |
| `src/data/dataLoader.h/.cpp` | 统一数据加载：技能/物品(装备)/角色/战斗配置/剧情 |
| `src/data/saveManager.h/.cpp` | JSON 存档（2 槽位，队伍 + 剧情进度元信息） |
| `src/combat/combatSystem.h/.cpp` | 战斗系统：回合流程、AI、状态效果结算、界面 |
| `src/combat/character.h/.cpp` | 角色：`Character`/`Civil`/`Combatant`，属性/状态/背包/装备槽位 |
| `src/combat/skill.h/.cpp` | 技能：伤害/治疗/充能 |
| `src/combat/item.h/.cpp` | 物品：消耗品/装备 |
| `src/combat/battleLog.h/.cpp` | 战斗日志 |
| `src/combat/combatTestMain.cpp` | 战斗测试入口 |
| `src/story/Game.h/.cpp` | 游戏外壳：主循环、指令处理、玩家/金币/物品池、存读档 |
| `src/story/SceneManager.h/.cpp` | 场景状态机、剧情播放、药店/锻造入口、战斗接入 |
| `src/story/SceneMap.h/.cpp` | 将四幕网格地图接入主游戏（WASD 交互循环） |
| `src/story/TalkManager.h/.cpp` | 对话系统（从 talk.json 加载） |
| `src/story/MapGrid.h/.cpp` | 网格地图系统：移动/碰撞/交互/建造/背包界面 |
| `src/story/gaiXiaMap.cpp` | 第一幕「垓下营地」地图展示 |
| `src/story/huaiRiverMap.cpp` | 第二幕「淮河突围」地图展示 |
| `src/story/dongchengMap.cpp` | 第三幕「东城快战」地图展示 |
| `src/story/wujiangMap.cpp` | 第四幕「乌江自刎」地图展示 |
| `src/story/mapTestMain.cpp` | 地图移动测试（遇敌接真实 CombatSystem） |
| `src/story/PharManager.h/.cpp` | 药店：购买/使用药品/查看背包 |
| `src/story/ForgeManager.h/.cpp` | 锻造（占位） |
| `src/story/map.h/.cpp` | 旧版静态字符地图（遗留） |
| `src/story/BackGround.h/.cpp` | 旧版场景文字标题（遗留） |
| `data/*.json` | 静态模板：剧情、技能、物品/装备、对话、角色、战斗配置 |

---

## 5. 待办事项

- [ ] **锻造**：实现 `ForgeManager` 并真正接入 `SceneManager::FORGE` 状态。
- [ ] **战斗内道具使用**：实现 `performItem`。
- [ ] **第四幕对话接线**：`SceneManager` TALK 状态 `case 4` 调用 `talkScene04`。
- [ ] **地图遇敌战斗按名匹配**：`SceneMap::onBattle` 目前统一走 `battle_test.json` + 存档位 1，应改为按 NPC 名称匹配对应 `battle_*.json`。
- [ ] **三套地图系统整合**：`BackGround` / `map_Manager`（旧静态字符地图）/ `MapGrid` + `SceneMap`（新网格地图）并存，需统一并清理前两者。
- [ ] **清理陈旧注释与调试残留**：`dataLoader.h:23`、`combatSystem.h:3-6`、`SceneManager.cpp` 的「未制作」注释，及 `ShowBackground`/`startStoryBattle` 的 DEBUG 打印。
- [ ] 任务系统。
- [ ] 补充自动化测试（目前为手工交互测试）。

---

## 6. 测试方法

### 6.1 构建

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

编译无报错即通过基础构建测试。共 7 个目标：`Chugeqi`、`combatTest`、`mapTest`、`gaiXiaMap`、`huaiRiverMap`、`dongchengMap`、`wujiangMap`。

### 6.2 剧情测试（`Chugeqi`）

1. 启动后输入 `1` 开始游戏，观察四幕剧情逐字播放。
2. 输入 `auto` / `manual` 切换自动/手动播放；播放中按 ESC 切换，按 q 加速。
3. 输入 `map` 查看世界地图；`south`/`north` 推进/回退剧情；`start` 回主界面。
4. 第二幕田父指路处，输入 `A`/`B` 验证两条分支（王翦/沼泽）剧情不同。
5. 剧情行进到「打斗」处会触发真实战斗（王翦/秦时月/最终决战），失败则剧情中断。
6. 进入场景后为网格地图：WASD 移动，走向 NPC 对话、药店购买、铁匠铺（占位）、遇敌战斗、拾取、幕次跳转点（帅帐/浅滩/突围/渡船）。

### 6.3 战斗测试（`combatTest`）

1. 普通攻击、技能（火球/治疗/聚力）、逃跑。
2. 状态效果：火球附加灼烧（回合开始扣血）、聚力进入充能（属性提升）、观察状态栏显示。
3. 敌人 AI：敌方会回击；按菜单开启我方 AI 托管，ESC 退出。
4. 存档：战斗结束后写回存档文件，再次运行验证等级保留；选 `Y` 重置。

### 6.4 存读档测试（`Chugeqi`）

1. 主菜单输入 `2`（继续征途）打开存读档界面，或地图内按 `F`。
2. 选择存档位 → 存档（覆盖需 y/n 确认）/ 读档 / 返回。
3. 读档后验证队伍等级、金币、场景进度（scene/branch）恢复。
4. 存档文件生成于 `saves/save_<slot>.json`，可直接查看 JSON 内容。

### 6.5 地图测试（`mapTest`）

1. WASD 控制项羽移动；边界墙壁不可穿越。
2. 碰到友方单位触发对话；碰到敌方单位触发**真实战斗**（接 `CombatSystem` + 存档读写）。
3. 碰到药店 / 铁匠铺触发对应交互。
4. ESC 退出。

### 6.6 地图展示（`gaiXiaMap` 等四个）

1. WASD 移动、B 打开背包（装配/卸下/使用）。
2. 拾取物品（ITEM 格）验证入包与消失。
3. 带任意参数运行只渲染一次即退出。

### 6.7 修改 JSON 数据测试

- 改 `data/story.json` 的文本/颜色/分支/`battleId`，重跑 `Chugeqi` 验证剧情与战斗变化。
- 改 `data/skill.json` 或 `data/player.json` 的数值，重跑 `combatTest` 验证战斗数值变化。
- 改 `data/item.json` 装备 `bonus` 或药水 `healHP`，重跑 `gaiXiaMap` 开背包验证。
- 改 `data/battle_*.json` 的 `enemies_ref`/`disable_run`，验证剧情战斗配置生效。

> JSON 语法错误会触发解析器异常，可借此验证错误处理。

---

## 7. 已知遗留 / 技术债

### 7.1 三套场景表现系统并存

| 系统 | 文件 | 现状 |
|------|------|------|
| 早期文字标题 | `BackGround.h/.cpp` | 仍被 `showCurrentBackground` 调用，功能弱，定位模糊 |
| 旧静态字符地图 | `map.h/.cpp`（`map`/`map_Manager`） | 仍被 `refreshScene` 调用，硬编码字符画 |
| 新网格地图 | `MapGrid` + `SceneMap` | 主游戏交互已走这里，四幕地图已接入 |

三套系统职责重叠，后续应统一为 `MapGrid`/`SceneMap`，移除或归档前两者。

### 7.2 陈旧注释（与现状不符）

- `dataLoader.h:23`：「`itemPool; // 道具未实现，保持为空`」—— 物品/装备已实现。
- `combatSystem.h:3-6`：「道具使用/状态效果/充能技能未实现」—— 状态效果与充能已实现，仅 `performItem` 未实现。
- `SceneManager.cpp`：「药店未制作」「锻造未制作」—— 药店已完成。

### 7.3 调试残留

- `SceneManager::ShowBackground` 内「第三幕 DEBUG」整段打印（scene id / lines size / line 文本）。
- `SceneManager::startStoryBattle` 内 `[DEBUG] battleId = ...` / `[DEBUG] battlePath = ...` 打印。

### 7.4 死代码 / 未接线

- `TalkManager::talkScene_020`（农夫）为无用死代码，菜单项 `1.`/`2.` 为空。
- `TalkManager::talkScene04` 已实现但 `SceneManager` TALK 状态 `case 4` 未调用。
- `SceneManager::FORGE` 状态内 `void forge();` 为局部声明而非调用，`forgeManager` 成员未使用。
- `Game.cpp` 内「不要忘记删除！！！」等临时注释残留。
- `SceneMap::onBattle` 未按 NPC 名称匹配战斗配置，统一使用 `battle_test.json`。

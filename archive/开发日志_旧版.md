# 开发日志

本文记录「楚歌起 (Chugeqi)」的开发进度与现状，面向项目成员。

---

## 1. 项目概述

「楚歌起」是以楚汉相争、项羽垓下之围至乌江自刎为背景的 Windows 控制台回合制 RPG，由两个项目合并而成：

- **Chugeqi**：战斗内核（回合制战斗、角色/技能/物品、SQLite 存档、JSON 数据加载）。
- **TheFalloftheOverlord**：剧情外壳（四幕剧情、场景、对话、地图、逐字打印）。

技术栈：C++17 + CMake + 自研 JSON 解析器 + SQLite（官方合并源码）。

核心设计思想是「数据分层」：

| 分层 | 内容 | 存储方式 |
|------|------|----------|
| 静态模板 | 技能池、物品池、剧情文本、敌人模板 | JSON 文件，启动时载入一次 |
| 运行时持久状态 | 队伍 HP/SP、等级/经验、技能/道具 | SQLite（`save.db`） |
| 临时数据 | 一场战斗的战斗者、场景状态 | 内存，随流程释放 |

---

## 2. 开发进度

### 2.1 战斗原型（Chugeqi 早期）

- 回合制战斗：玩家 + 同伴手动操作，普攻 / 伤害技能 / 治疗技能 / 逃跑。
- 命中与伤害计算：基于敏捷的命中率、`力量 × 威力 − 防御` 的伤害公式。
- 角色成长、技能/物品类层次、自研 JSON 解析器、SQLite 三表存档。
- 平台抽象层 `console`（清屏 / 按键 / 编码）。

### 2.2 状态效果大修

- 实现灼烧 / 迟缓 / 眩晕 / 充能四种状态效果。
- 结算统一到战斗主循环每轮开始（灼烧扣血、迟缓提示）与轮末（持续回合递减）。
- 属性修正（迟缓降敏捷、充能提属性）集中在 `Combatant::getEffectiveStat`。
- 保留并完善了敌人 AI（回击、技能选择、集火残血）与我方 AI 托管。

### 2.3 TheFalloftheOverlord 移植

- 全源码 GBK → UTF-8，移除 `windows.h` / `conio.h` 依赖，接入 `console` 平台抽象。
- `console` 层扩展：新增 `setColor` / `sleep` / `kbhit`（含非 Windows 分支）。
- 构建由 `.sln` / `.vcxproj` 改为 CMake。
- 剧情文本抽离为 `data/story.json`，由 `loadStory` 解析（旁白/对话/分支结构）。

### 2.4 合并两个仓库

- 用 `git merge --allow-unrelated-histories` 把 TheFalloftheOverlord 合并进 Chugeqi，保留两个仓库的完整提交历史。
- 统一 console 层（8 函数版）、去重 `json.h`、合并 CMakeLists、以 `main.cpp`（剧情）为主入口。

### 2.5 目录重构 + 统一数据加载

- 源码按职责拆为 `src/core/`（平台抽象 + 数据加载）、`src/combat/`（战斗）、`src/story/`（剧情）。
- 剧情数据结构（`StoryLine` / `Scene` / `StoryData`）与 `loadStory` 并入 `DataLoader`，实现战斗与剧情的统一加载入口。
- CMake 拆出两个目标：`Chugeqi`（主游戏）与 `combatTest`（战斗测试）。

---

## 3. 当前功能现状

### 3.1 剧情系统（`src/story`）

- 四幕剧情（垓下/阴陵/东城/乌江），从 `data/story.json` 加载。
- `SceneManager` 负责场景状态机与剧情播放：`printWords` 逐字打印、`nextLine` 自动/手动切换（ESC）。
- 第二幕分支（田父指路 A/B）由 `StoryChoice` / `StoryOption` 表达。
- `TalkManager` 提供场景内选择角色对话，对话内容为占位（「没写」）。

### 3.2 战斗系统（`src/combat`）

- 回合流程：玩家 → 同伴 → 敌方，循环至一方全灭或逃跑。
- 普通攻击、伤害/治疗/充能技能、逃跑。
- 敌人 AI：回击，约 60% 概率施放技能、集火残血；我方支持 AI 托管（ESC 退出）。
- 状态效果：灼烧（回合开始扣血）、迟缓（敏捷 ×3/5）、眩晕（跳过行动）、充能（属性 ×倍率）。
- 经验与升级、SQLite 存档。

### 3.3 平台抽象（`src/core`）

- `console`：`init` / `clearScreen` / `readKey` / `pause` / `pauseEsc` / `setColor` / `sleep` / `kbhit`。
- `json.h`：轻量递归下降 JSON 解析器（只读）。
- `dataLoader`：统一加载技能、角色、战斗配置、剧情。

---

## 4. 目录结构与文件职能

| 文件 | 职能 |
|------|------|
| `src/main.cpp` | 主入口：创建 `Game` 并运行剧情 |
| `src/core/console.h/.cpp` | 平台抽象：清屏、按键、颜色、延时、编码初始化 |
| `src/core/json.h` | 轻量 JSON 解析器 |
| `src/core/dataLoader.h/.cpp` | 统一数据加载：技能/角色/战斗配置/剧情 |
| `src/combat/combatSystem.h/.cpp` | 战斗系统：回合流程、AI、状态效果结算、界面 |
| `src/combat/character.h/.cpp` | 角色：`Character`/`Civil`/`Combatant`，属性/状态/背包 |
| `src/combat/skill.h/.cpp` | 技能：伤害/治疗/充能 |
| `src/combat/item.h/.cpp` | 物品：消耗品/装备 |
| `src/combat/saveManager.h/.cpp` | SQLite 存档 |
| `src/combat/battleLog.h/.cpp` | 战斗日志 |
| `src/combat/combatTestMain.cpp` | 战斗测试入口 |
| `src/story/Game.h/.cpp` | 游戏外壳：主循环、指令处理 |
| `src/story/SceneManager.h/.cpp` | 场景状态机、剧情播放 |
| `src/story/TalkManager.h/.cpp` | 对话系统 |
| `src/story/map.h/.cpp` | 世界地图 / 场景地图 |
| `src/story/BackGround.h/.cpp` | 场景背景 |
| `src/story/ForgeManager.h/.cpp` | 锻造（占位） |
| `src/story/PharManager.h/.cpp` | 药房（占位） |
| `third_party/sqlite/` | SQLite 官方合并源码 |
| `data/*.json` | 静态模板：剧情、技能、角色、战斗配置 |

---

## 5. 待办事项

- [ ] **剧情 ↔ 战斗衔接**：剧情里三处「打斗」（第二幕王翦、第三幕斩将、第四幕轮番战）接入 `CombatSystem`。
- [ ] 对话内容：`TalkManager` 各角色对话填充。
- [ ] 药房 / 锻造：实现 `PharManager` / `ForgeManager`。
- [ ] 道具使用：`performItem` 实现。
- [ ] 任务系统。
- [ ] 剧情存档（当前仅战斗有 SQLite 存档，剧情进度未持久化）。
- [ ] 补充自动化测试（目前为手工交互测试）。

---

## 6. 测试方法

### 6.1 构建

```powershell
cmake -S . -B build -G "Visual Studio 18 2026" -A x64
cmake --build build --config Release
```

编译无报错即通过基础构建测试。

### 6.2 剧情测试（`Chugeqi.exe`）

1. 启动后输入 `1` 开始游戏，观察四幕剧情逐字播放。
2. 输入 `auto` / `manual` 切换自动/手动播放；播放中按 ESC 切换。
3. 输入 `map` 查看地图；`south`/`north` 推进/回退剧情；`start` 回主界面。
4. 第二幕田父指路处，输入 `A`/`B` 验证两条分支（王翦/沼泽）剧情不同。

### 6.3 战斗测试（`combatTest.exe`）

1. 普通攻击、技能（火球/治疗/聚力）、逃跑。
2. 状态效果：火球附加灼烧（回合开始扣血）、聚力进入充能（属性提升）、观察状态栏显示。
3. 敌人 AI：敌方会回击；按菜单开启我方 AI 托管，ESC 退出。
4. 存档：战斗结束后写回 `save.db`，再次运行验证等级保留；选 `Y` 重置。

### 6.4 修改 JSON 数据测试

- 改 `data/story.json` 的文本/颜色/分支，重跑 `Chugeqi.exe` 验证剧情变化。
- 改 `data/skill.json` 或 `data/player.json` 的数值，重跑 `combatTest.exe` 验证战斗数值变化。

> JSON 语法错误会触发解析器异常，可借此验证错误处理。
